services:
  - type: web
    name: my-node-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    repo: https://github.com/saka015/alumnity-be
    branch: main
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
